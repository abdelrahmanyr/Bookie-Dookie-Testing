[pytest]
DJANGO_SETTINGS_MODULE = Bookie_Dookie_Dj.settings
testpaths = tests
python_files = test_*.py
addopts = -q -m "not known_issue"
markers =
	known_issue: tests that represent current known defects and are expected to fail
